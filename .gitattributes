# 改行はLFで統一する（同じファイルがWindows/Macで別物にならないように）
* text=auto eol=lf

# バイナリ扱い。改行変換で壊れるファイル
*.pdf  binary
*.png  binary
*.jpg  binary
*.ico  binary
*.woff binary
*.woff2 binary
*.gz binary
*.wasm binary
*.traineddata binary
*.wasm.js -diff
